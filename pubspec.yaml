name: booked_utils
description: >
  A collection of reusable Flutter utilities: typedefs, wrappers, extensions, and helpers for rapid development across multiple projects.
version: 1.1.2
homepage: https://github.com/ddarkbooked/booked_utils
repository: https://github.com/ddarkbooked/booked_utils

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

topics:
  - utils
  - flutter
  - extensions
